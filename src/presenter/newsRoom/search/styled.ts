import styled from "@emotion/styled";

const Section = styled.section`
  padding-top: 50px;
`;

const Article = styled.article``;

const TitleBox = styled.div`
  margin: 80px auto;
  margin-bottom: 20px;
`;

const Title = styled.h3`
  text-align: center;
  color: #232324;
  font-size: 42px;
  font-weight: 700;
  line-height: 1.5238095238;
`;
const InputBox = styled.div`
  width: 400px;
  margin: 0 auto 80px;
`;

const Input = styled.input`
  width: 100%;
  padding: 0 25px 0 0;
  font-size: 20px;
  line-height: 1.6;
  border-width: 0 0 1px 0;
  border-color: #232324;
  border-radius: 0;
  background-color: white;
`;

export { Section, Article, TitleBox, Title, InputBox, Input };
